services:
  backend:
    build:
      context: .
      dockerfile: infra/backend/Dockerfile
    env_file:
      - .env
    ports:
      - "${BACKEND_PORT:-18437}:18437"
    volumes:
      - ${NEWSLETTER_IMPORT_ROOT_HOST:-./Newsletter/output}:${NEWSLETTER_IMPORT_ROOT_CONTAINER:-/mnt/import/newsletters}:ro
      - ./storage:/app/storage
      - ./backend/data:/app/backend/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:18437/api/v1/health')"]
      interval: 10s
      timeout: 5s
      retries: 10

  frontend:
    build:
      context: .
      dockerfile: infra/frontend/Dockerfile
    env_file:
      - .env
    ports:
      - "${FRONTEND_PORT:-29501}:29501"
    depends_on:
      backend:
        condition: service_healthy
