"""office-tools 도구 서비스 계층.

각 도구(보고서/차트/다이어그램)의 생성 로직을 라우터에서 분리해 여기에 둔다.
서비스는 JobStore 와 (선택) LLM 클라이언트를 받아 산출물을 만들고, 렌더는 하지
않는다(브라우저 렌더 결정, BUILD_CONTRACT §2.5). 지금은 다이어그램 스튜디오
(``diagram_service``)가 채워져 있다.
"""
