# AeroOne 1.16.3 전수 검사 리포트 (2026-07-16)

- 기준 커밋: `3978cb1` (release-1.16.3-main) + 본 사이클 leantime 수정 `678440a`
- 검사 방식: 소스 전량 열람(주요 표면 정독) + 실기동 재현(Leantime 스택 E2E) + 자동화 게이트 실행
- 자매 문서: [`v1-17-0-improvement-plan-2026-07-16.md`](v1-17-0-improvement-plan-2026-07-16.md) (개선 계획)

---

## 0. 검증 게이트 실측 결과

| 게이트 | 결과 | 비고 |
|---|---|---|
| backend `pytest tests` (1.16.3 원본) | **28 failed / 1,150 passed** | 1.16.3 은 회귀 게이트가 빨간 상태로 릴리스됐다 — §13 참조 |
| backend `pytest tests` (테스트 수리 후) | **1,179 passed / 0 failed** (unit 834 + integration 345) | 스테일 테스트 28건을 1.16.3 정책에 맞게 수리 + leantime 회귀 7건 신규 |
| frontend `vitest run` | **540 passed / 88 files** | 문서상 397/73 표기는 구버전 수치 |
| frontend `tsc --noEmit` | **통과** | 단, 검사 시작 시점엔 로컬 node_modules 에 echarts/mermaid 누락으로 실패 → `npm install` 후 통과 (환경 드리프트, §13) |
| Leantime 스택 실기동 E2E | **통과** (수정 후) | setup→start→문서화 비밀번호 로그인 303→dashboard 200→stop |

> **중대 발견 — 릴리스된 1.16.3 의 백엔드 스위트는 28건 실패 상태였다.** 실패 전량이 1.16.3 의
> 의도된 정책 변경("발급 계정 전체 접근" — user 역할 기본권한에 `office.use`/`leantime.read` 등
> 추가, 커밋 `32f6194`)을 반영하지 못한 **스테일 테스트**였고, 제품 동작 자체는 changelog 의
> 의도와 일치했다(권한 과다 부여 아님 — `admin.*` 관리 권한은 여전히 관리자 전용임을 재검증).
> 직전 커밋 Directive("정식 릴리스 전 backend 전체 pytest 전량 실행")가 이행되지 않은 결과다.
> 본 사이클에서 28건 전량을 새 정책에 맞게 수리했다: 권한 없는 인증 주체의 403 경로는 역할
> 기본 권한이 빈 `pending` 계정으로 재현하도록 픽스처를 바꾸고(`test_office_tools_{auth,jobs}.py`,
> `test_leantime_adapter.py`), 정책 단언을 "발급 계정 기본 보유 + 관리 권한은 admin 전용"으로
> 갱신했다(`test_admin_permissions.py`, `test_auth_activity_api.py`). AGENTS §8 의 570/397 수치도
> 구버전이다(실측 1,179/540).

---

## 1. 폐쇄망 배포·설치 파이프라인 — **86/100**

**강점.** allow-list `git archive` → clean build → Task 5 pre/post ZIP 검증, exact annotated tag 없으면 fail-closed QA 산출물, sha256 게시, Sandbox 스모크(`run_offline_package_smoke.ps1`), 자격증명 회전 모듈 24종, 유지보수 게이트 97/98 분화(1.16 사이클 수정). 이 규모의 개인 프로젝트에서 보기 드문 수준.

**결함 (실측).**
- **[치명, 수정됨]** Leantime 스택 `setup-leantime-stack.bat` 이 블록 안 비이스케이프 괄호(`(skip).`)로 **파스 사망**(`. was unexpected at this time.`) — 이번 사이클에서 본 PC 재현·수정·E2E 검증 완료(`678440a`). 게시된 1.16.3 Release 의 스택 asset(`…20260715-193631.zip`)은 **깨진 판**이므로 hotfix 릴리스에서 교체 게시 필요.
- **[치명, 수정됨]** `EnableDelayedExpansion` 이 기본 관리자 비밀번호의 `!` 를 제거 — 문서화된 `AeroOneLean2026!` 로 로그인 불가 상태로 시드.
- **[중]** 프로세스 갭: "⚠ 운영자 검증 필요" 주석이 달린 반입물이 E2E 미실행 상태로 릴리스에 실렸다. *반입물은 최소 1회 본 PC 실기동 후 게시* 게이트가 없다.
- **[하]** CI 부재 — 모든 게이트가 수동 실행. 위 두 치명 결함 모두 "실행되면 즉시 드러나는" 부류였다.

## 2. 대시보드(홈) — **82/100**

**강점.** 섹션(뉴스레터/문서/AI/개발/기타) 재편(1.16.0), 서버측 RBAC 필터(fallback 시 보수적 규칙), SSR 병렬화(`Promise.all`), degraded 배너, 카드 배지·정렬 DB 구동.

**개선 여지.**
- 카드가 유일한 진입 UI — 최근 본 문서/뉴스레터, 통합 검색(이미 admin 엔드포인트 존재)의 홈 노출이 없다. "오늘 볼 것"이 없는 정적 런처.
- `Development` 섹션명이 일반 사용자에게 개발 은어. 발급 계정 전체 접근(1.16.3) 이후로는 더 그렇다.
- Coming soon 카드(Announcement/Schedule)가 수개월째 자리 차지.
- 외부 앱 카드(Notebook/OpenWebUI)는 Leantime 과 달리 **健康 배지 없음** — 눌러야 죽은 링크인지 안다.

## 3. Newsletter — **85/100**

달력·카테고리·읽음추적·다운로드 강조·테마·자동 동기화(재시작 불필요)까지 성숙. sanitize 단일 원천(`HtmlRenderService`) + sandbox iframe 정책 일관.
**개선 여지:** 본문 내 검색/하이라이트 없음(FTS 는 AI·admin 쪽만), 이슈간 이전/다음 내비 없음, 대량 이슈 시 달력 외 목록 뷰 빈약.

## 4. 컬렉션 (Document / NSA) — **80/100**

same-origin 프록시로 외부 PC "failed to fetch" 재발 원천 차단(C1 회귀가드), 폴더 트리, deep-link(`?path=`), `_debug` 제외, path-guard 400/404 계약, NSA fail-closed(모듈 disabled/hidden 시 admin 도 차단 — 1.13 리팩터).
**개선 여지:** NSA `0000` 클라이언트 가림막은 여전히 장식(문서화된 한계지만 1.8+ RBAC 이 생긴 지금은 `collections.nsa.read` 권한 게이트로 승격할 때가 됐다). 문서 목록에 수정일/크기 메타 없음, 미리보기 썸네일 없음, 트리 상태 미보존(새로고침 시 리셋).

## 5. Civil Aircraft Spec Catalog v1.7 — **84/100** (집중 연구)

**강점.** 출처 거버넌스가 백미 — 55개 출처 레지스트리 + PDF 25건 SHA-256/페이지 검증 + `BUILD_INTEGRITY`/`SHA256SUMS`, QA 64+41+12 PASS 기록. 미공개 치수는 **추정하지 않고** "미공개" SVG 폴백(비추정 원칙). iframe 격리 + 번들 내 스크립트만 실행(폐쇄망 순도). v1.7 에서 라벨 겹침·가로 스크롤·거버넌스 상단 배치 해결.

**SVG 시스템 평가 (`aircraft-svg.v1.7.js`).**
- 구조: 파라메트릭 실루엣(top/side/front, 공통축척 overlay/compareSheet, `fixedScaleFor`) — 좋은 뼈대. `role="img"`+`<title>`+`<desc>` a11y 준수.
- 한계 1 — **형상 충실도**: 모든 기체가 같은 제네릭 경로(동체 Q-커브+사다리꼴 날개)에 파라미터만 바뀐다. A380 humpLengthPct 같은 단서는 있으나 747/A350/ATR 구분이 실루엣만으로 약함.
- 한계 2 — **상호작용 없음**: hover 치수 툴팁, 기체 hover 시 overlay 내 강조, 클릭→백과 이동이 없다. 문자열 조립 SVG 라 이벤트 부착 지점도 없음.
- 한계 3 — **내보내기 없음**: 비교 시트를 PNG/SVG 로 저장하는 버튼이 없다 (Office 차트는 PNG 다운로드가 있는 것과 비대칭).
- 한계 4 — **로딩**: `civil-aircraft-data.v1.7.js` 330KB + data JSON 485KB 를 진입 즉시 로드. 폐쇄망 LAN 이라 치명적이진 않으나 첫 페인트 지연 요인.
- 한계 5 — side view 에 날개 스윕 미반영, 엔진 파일런 단순화 — v1.7 REVISION 의 "not a controlled drawing" 명시로 면책은 됨.

## 6. AeroAI — **78/100**

문서 근거(FTS) 검색, citation 패널·딥링크, 프리셋 6종, 대화 영속화, provider 이원 관리(DPAPI, write-only 키, no-fallback), scope 의 NSA 기본 비노출. 보안 설계 상급.
**개선 여지:**
- **비스트리밍**(`stream: False`) — 로컬 LLM 특성상 긴 침묵 후 한 번에 출력. 체감 품질 최대 저해 요인.
- 파일 첨부 질문 불가(컬렉션 문서만 근거) — Office 차트 대화형 개편과 같은 결에서 "첨부한 파일에 대해 묻기"가 없다.
- 41KB 단일 워크스페이스 컴포넌트 — 기능 추가마다 비대해지는 구조.

## 7. Office Studio — 차트 — **74/100** (사용자 지적 표면)

**강점.** 파일/텍스트 이원 입력, 예제 원클릭 생성, 규칙기반↔LLM 이중 엔진, 서버 재검증, 고급 설정의 결정적 manualSpec, 이력 재열기/복제, 안전한 산출물 경로 검증(regex pin), PNG/zip 다운로드. 엔지니어링은 견고.

**UX 진단 — "입력란이 복잡해 보인다"는 지적은 정확하다.**
- 첫 화면에 동시 노출되는 결정 요소가 **8개+**: 사용 가이드, 예제 피커, 모드 토글, 파일 입력, 텍스트 영역, 목적 문장, 유형 select, AI 체크박스, 미리보기/생성 2버튼. Step 1~3 라벨이 있어도 폼 전체가 한 번에 펼쳐져 위저드가 아니라 설정 패널로 읽힌다.
- "데이터 미리보기"와 "차트 생성"이 병렬 버튼 — 무엇을 먼저 눌러야 하는지 모호(미리보기는 고급 설정의 전제조건인데 그 관계가 숨어 있음).
- 목적 문장(프롬프트)이 한 줄 `<input>` — LLM 대화 멘탈모델과 어긋난다.
- 생성 후 수정은 "옵션 바꾸고 다시 생성" — 대화형 반복("막대를 가로로", "상위 5개만")이 불가.

→ 개선 계획 P1-1 "대화형 컴포저" 참조 (텍스트박스 + 드래그앤드롭 첨부 + 후속 명령 대화, 고급 패널은 접힘 유지).

## 8. Office Studio — 보고서·다이어그램 — **80/100**

보고서(Markdown→이미지 내장 단일 HTML)와 다이어그램(Mermaid 4종)은 차트보다 입력 표면이 단순해 UX 부채가 덜하다. 멀티파트 ingress 한도 미들웨어, 작업 이력·복구·격리 계약(86KB `jobs.py`, 잘 작동하나 §13 크기 문제). 다이어그램도 대화형 반복 부재는 동일.

## 9. Leantime 동거 통합 — **검사 시점 45 → 수정 후 78/100**

경계 설계(HTTP-only 연동, 세션/DB 미공유, 서버측 JSON-RPC 읽기, 상태 5단계 배지, LAN-aware 열기 버튼)는 우수. 그러나 **핵심 가치인 "실제로 뜬다"가 게시판에서 죽어 있었다** — 파스 사망 + 비밀번호 훼손 + localhost APP_URL 3중 결함(§1, 수정·검증 완료). `/leantime` 페이지의 IIS 설치 안내문 드리프트도 이번에 교정. 남은 것: 폐쇄망 실 PC 재검증 + hotfix 게시.

## 10. 관리자 콘솔 — **83/100** (리모델링 관점)

**강점.** 9탭 + 개요 위젯, RBAC 매트릭스/자원 grant, 세션 fanout·자동 새로고침, 감사 CSV, 백업, 숫자 단축키, 페이지네이션, red-team 테스트(g001~g004)까지 — 기능 완결성 높음.

**리모델링 포인트.**
- **IA**: '시스템' 탭에 health + config + AI provider + LLM 연결이 동거 — 관심사 4개가 한 탭. '검색' 탭은 고립. 사용자/RBAC/세션은 셋 다 "계정" 관심사인데 3탭 분산.
- **성능**: 마운트 시 refresh key **17종 전부 fetch** — 개요만 보고 나가는 관리자도 17 요청. 탭 진입 시 lazy fetch 로 바꿀 표면.
- **코드**: `admin-console-tabs.tsx` 43.9KB + `admin/api.py` 50.9KB — 섹션 분해가 진행 중이나 컨테이너가 여전히 거대.

## 11. 인증·권한·보안 — **88/100**

APP_ENV 4모드 + secret 강도 검증, CSRF, RBAC(권한/그룹/자원 grant), 감사 로그, sanitize 단일 원천, AI egress fail-closed(신뢰 TLS 또는 loopback), DPAPI 키 보호, write-only 시크릿, 세션 해시. 위험 신호 6종(AGENTS §6) 모두 준수 확인.
**잔여:** NSA 가림막(§4), closed_network HTTP 평문(수용된 트레이드오프, 문서화됨), bcrypt 4.0.1 핀은 passlib EOL 리스크 — 장기적으로 passlib 이탈 검토.

## 12. 성능 — **70/100**

**현황.** SQLite+FastAPI 는 팀 규모에 적정. SSR 병렬화, 차트 echarts 지연 import, iframe 격리로 메인 번들 보호 등 낱개 최적화는 있음. v1.13 QA 하니스에 Lighthouse 러너까지 있으나 **성능 예산(budget)과 정기 실측 기록이 없다.**
**구체 이슈.**
- 관리자 콘솔 17-fetch 문제(§10).
- Civil 진입 시 815KB(JS+JSON) 즉시 로드(§5).
- AI 비스트리밍 → 최악 수십 초 무반응(§6).
- Leantime PHP built-in server 는 Windows 에서 **단일 워커** — 동시 사용자 몇 명이면 직렬화. 소규모 팀 한정 수용 가능하나 문서화 필요.
- Office 차트/보고서 생성이 요청 스레드에서 동기 실행 — 큰 xlsx 에서 워커 점유. (스레드풀이라 이벤트루프는 안 막힘.)

## 13. 코드 품질·테스트·문서 — **78/100**

**강점.** backend 1,179/frontend 540 그린(수리 후), red-team 스위트, 배치 스크립트까지 테스트(이번 사이클 leantime 7건 추가), phase 보고서 아카이브는 모범.
**부채.**
- **[중대, 수리됨] 릴리스 시점 빨간 스위트**: 1.16.3 은 backend 28 failed 상태로 태그·게시됐다(§0). 정책 변경(발급 계정 전체 접근)과 테스트 갱신이 분리된 채 전체 스위트 미실행으로 병합된 프로세스 결함 — 본 사이클에서 28건 전량을 새 정책 기준으로 수리했고, 개선 계획 P0-2(전체 게이트 의무화)로 재발을 막는다.
- **거대 파일**: `office_tools/api/jobs.py` 2,355줄, `admin/api.py` 50.9KB, `chart-form.tsx` 897줄, `ai-chat-workspace.tsx` 41KB, `admin-console-tabs.tsx` 43.9KB.
- **문서 수치 드리프트**: AGENTS §8 "570/397" vs 실측 1,179/540. `/leantime` IIS 안내(수정됨). README 반입 절은 hotfix 시 갱신 필요.
- **테스트 소음**: AccountMenu `act()` 경고 다수, pydantic V2 deprecated config 3건, pytest-asyncio loop_scope 미설정 경고.
- **환경 드리프트**: 본 개발 PC node_modules 가 package.json 과 불일치(echarts/mermaid 누락) — lockfile 기준 `npm ci` 습관/스크립트 부재.

## 14. 외부 앱 연동 (Notebook / OpenWebUI) — **75/100**

링크 전용 경계(iframe/헬스 프록시 미사용)는 의도된 결정으로 문서화 완비. 다만 Leantime 만 상태 배지가 있고 Notebook/OpenWebUI 는 무배지 — 죽은 링크 클릭 후에야 알게 되는 비대칭. run_all co-boot 는 견고.

---

## 종합 — **80/100**

| # | 섹션 | 점수 |
|---|---|---:|
| 1 | 폐쇄망 배포·설치 | 86 |
| 2 | 대시보드 | 82 |
| 3 | Newsletter | 85 |
| 4 | 컬렉션(Document/NSA) | 80 |
| 5 | Civil v1.7 (+SVG) | 84 |
| 6 | AeroAI | 78 |
| 7 | Office 차트 | 74 |
| 8 | Office 보고서·다이어그램 | 80 |
| 9 | Leantime 통합 | 45→78 |
| 10 | 관리자 콘솔 | 83 |
| 11 | 인증·보안 | 88 |
| 12 | 성능 | 70 |
| 13 | 품질·테스트·문서 | 78 |
| 14 | 외부 앱 연동 | 75 |

**한 줄 진단:** 보안·검증·문서화 설계는 상용 수준을 넘는데, (a) "실제 사용자가 매일 만지는 손맛"(대화형 입력, 스트리밍, 상태 가시성, 첫 화면의 판단 비용), (b) "반입물 실기동 게이트", (c) "릴리스 전 전체 스위트 의무 실행" 세 축이 뒤처져 있다 — (b)(c)는 이번 사이클에서 결함으로 실증됐다. 개선 계획은 이 세 축에 집중한다.
