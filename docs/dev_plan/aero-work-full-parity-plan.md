# Aero Work — gongmuwon 완전 기능 파리티 구현 계획

- 상태: **계획 확정용 · 실물 대조 기반** — 2026-07-19 작성
- 관계: 본 문서는 [`aero-work-plan.md`](aero-work-plan.md)(초기 6기능 매핑)를 **심화·대체**한다. 초기 계획은 6메뉴 골격만 다뤘고, 그 결과 **폭(7메뉴 존재)은 갖췄으나 깊이·핵심 오케스트레이션·UX 를 놓쳤다**(중간 검토: [`../reports/aero-work-midterm-review.md`](../reports/aero-work-midterm-review.md)). 본 문서는 **실제 gongmuwon 앱(README + 상세 사용설명서)을 항목별로 조사**해 진실 원천 체크리스트를 만들고, 완전 파리티까지의 단계를 정의한다.
- 진실 원천: gongmuwon `Kminer2053/gongmuwon`(README) + 사용설명서 `https://kminer2053.github.io/gongmuwon/manual/gongmu-user-manual.html`(11장). 라이선스: 앱 코드 MIT이나 **기능·UX 를 참고해 AeroOne 스택으로 독립 구현**(코드 이식이 아니라 재구현).
- 범위 판정: 인텐트 오케스트레이터·문서 양식 엔진·지식위키·캘린더/알림 등 신모듈 다수 → **major 릴리스(2.0.0)** 트랙. 다중 phase·다중 검증 게이트.

---

## 1. 현재 상태(스캐폴딩 완료분, `aero-work-dev`)

| 메뉴 | 현재 구현 | 깊이 |
|---|---|---|
| 🏠 홈 브리핑 | 일정+지식 요약 카드(`HomeBriefing`) | 얕음(정적 카드) |
| 💬 업무대화 | AeroAI(`AiChatWorkspace`) 재사용 | **인텐트 라우팅 없음** |
| 📅 일정 | 이벤트 CRUD + 아젠다 리스트 | 월/주/일·알림 없음 |
| 📝 문서작성 | 제목+본문 → 범용 HWPX | 양식·개조식·미리보기·승인 없음 |
| 📚 지식폴더 | 등록·재색인·코사인검색·삭제(txt/md/html) | 위키·대시보드·FTS·HWP/PDF 없음 |
| 🧾 실행기록 | 행위 자동 기록 + 타임라인 | 대체로 충족 |
| ⚙️ 환경설정 | AI 상태 확인 + 사용법 | 프로필 전환 없음 |

백엔드 `app/modules/aero_work`(마이그레이션 `20260719_0020~0022`) + 테스트 36건 + tsc 0. **골격은 견고하나, gongmuwon 정체성인 "대화 중심 오케스트레이션"과 각 기능의 실무 깊이가 비어 있다.**

---

## 2. gongmuwon 실물 기능 인벤토리 (진실 원천 체크리스트)

사용설명서 3~11장에서 추출. 각 항목이 파리티의 수용 기준이다.

### 2.0 전역 셸 / UX
- [ ] 헤더: 화면 배율(−/100%/＋), 새로고침, 우측 패널 접기, **업무 엔진 상태(초록/적색)**
- [ ] 4영역 레이아웃(헤더·좌측 메뉴·중앙·**우측 컨텍스트 패널**)
- [ ] **우측 패널**: 현재 컨텍스트 · 대기 중인 승인 · 최근 작업 · 가까운 일정(배지) · 색인 로그
- [ ] **첫 실행 3단계 온보딩**(①LLM 연결 ②지식폴더 등록 ③첫 대화)
- [ ] '응답 맥락' 초기화 / 새 세션

### 2.1 업무대화 (핵심 — 인텐트 오케스트레이션)
- [ ] 세션 목록 + 새 세션, 실시간 스트리밍(보유: AeroAI)
- [ ] "이렇게 말해보세요" 예시 힌트(클릭 시 입력)
- [ ] **인텐트 라우팅**: 발화 분류 → 일정등록/일정조회/일정삭제/문서작성/지식근거답변/기능도움말
- [ ] **멀티 인텐트**: "등록하고 작성해줘" → 작업 분해, `## 섹션` 으로 결과 병렬 표시
- [ ] **지식폴더 근거 답변**: 출처 칩 + **최신본 판별** + '지식위키 근거 N개'
- [ ] **대화→문서작성 이어가기**: 파일 열기·폴더 열기·검토용 Markdown 경로 링크
- [ ] 파일·**클립보드 이미지** 첨부

### 2.2 문서작성
- [ ] 5 양식: **시행문 / 1페이지 보고서 / 풀버전 보고서 / 이메일 / 임의형식(양식 빈칸 채움)**
- [ ] 작성 방법: 업무대화 세션 기반 / 직접 작성
- [ ] **구조 생성 → 검토**(개조식 □/◦, 1.·가.·1), Ⅰ.·Ⅱ. 위계 AI 생성)
- [ ] 미리보기 탭(평문 위계) + **수정 지시**로 재생성
- [ ] **양식(종이) 미리보기**(HWPX 서식 근사 렌더)
- [ ] **최종 저장 → 승인 → 적용**(승인형 정책)
- [ ] 탭 구성: 출력유형·작성방법·참고자료 / 작성 콘텐츠 / 미리보기 / 최종

### 2.3 내 지식폴더
- [ ] 3탭: **대시보드 · 위키 · 설정**
- [ ] 대시보드: 등록/위키 구성/색인/LLM 요약 상태 카드
- [ ] 설정: 지식 소스 등록(이름·경로·폴더 선택), **분류체계 마법사 3단계**(니즈→분류검토→적용)
- [ ] 색인 추출: **HWP/HWPX·PDF·Word**(현재 txt/md/html만) + 추출 상태·파서·품질·구조 보기
- [ ] 검색 2방식: **키워드(즉시 목차 강조, FTS)** / **상세검색(LLM 근거 스트리밍, 출처 품질순)**
- [ ] **지식위키**: 트리(업무/주제/폴더) · 문서 카드(요약·품질·목차·키워드·주제·미리보기 팝업) · 주제 페이지 · **업무 허브(대표 공식본 + 버전 이력 접힘)**
- [ ] 증분: 추가·수정·**이동(재파싱 없이 승계)**·**삭제(소프트삭제 30일)**

### 2.4 일정
- [ ] **월(6주 그리드) / 주(07~20시 시간축) / 일** 보기 + 이전·오늘·다음
- [ ] 빈 칸 클릭 → 시작·종료 자동 채움
- [ ] **사전 알림**: 없음/10분/30분/1시간/하루 전, 🔔 표기
- [ ] 알림 표기: **상단 배너("곧 시작 N분 후" + 확인/세션 열기)** · 우측 '가까운 일정' 배지 · 토스트

### 2.5 실행기록
- [x] 모든 작업 이력 추적(대체로 충족) — 입력·출력 상세 보강 여지

### 2.6 환경설정
- [ ] **모델 연결 프로필**(로컬 Ollama / 내부 서버 OpenAI 호환 / 외부 API) — 새 프로필 · [이 프로필 사용] 전환
- [x] 사용법(튜토리얼) 다시 보기

### 2.7 홈 브리핑(부가)
- [ ] 일정 · **이어서 하기(최근 세션)** · 지식 요약 · **앱 이용팁(일러스트)**

---

## 3. 목표 아키텍처 (오케스트레이션 중심 재편)

gongmuwon 의 정체성은 "대화 한 줄이 일정·문서·지식으로 이어지는 것"이다. 따라서 각 탭을 독립 CRUD 로 두지 말고 **인텐트 오케스트레이터**가 중심에 서야 한다.

- **백엔드** `app/modules/aero_work/orchestrator/`:
  - `intent_router`: 발화 → 인텐트 분류(규칙 우선 + LLM 보조). gongmuwon 발화표(§2.1) 재현.
  - `intent_handlers`: schedule/document/knowledge/help 실행 + 멀티 인텐트 분해.
  - AeroAI(스트리밍·provider) + 지식폴더(P2) + 일정(P4) + 문서(HWPX) 를 **호출 대상 서비스**로 연결.
- **문서 엔진** `document/`: 양식별(시행문/1p/풀버전/이메일/임의형식) 개조식 구조 생성기 + OWPML 양식 템플릿 + 승인 상태기계.
- **지식위키** `knowledge/`: FTS(SQLite FTS5 트라이그램) + 본문 추출기(HWP/HWPX/PDF/DOCX) + 위키 파생(분류체계·업무허브·버전 가족).
- **프런트**: 셸에 우측 컨텍스트 패널 + 온보딩 + 탭별 심화 뷰. AeroOne 디자인 토큰으로 gongmuwon UX 를 재현(픽셀 클론 아님).

---

## 4. 단계 계획 (F1~F8, 우선순위 순)

각 단계는 **AeroOne 자산 매핑 + 신규 + 검증 게이트 + phase 보고서**를 포함한다.

- **F1 — 인텐트 오케스트레이터 ✅ 구현됨**: 발화 → 인텐트 분류(규칙 기반) + 한국어 자연어 날짜·시각 파싱 + 멀티인텐트 분해 → 일정(등록/조회/삭제)·문서작성 인식·도움말·지식검색 라우팅. `korean_datetime`·`intent_router`·`orchestrator_service`(기존 Schedule/Knowledge 서비스 재사용) + `POST /orchestrate`(CSRF·실행기록) + `WorkChatPanel`(명령+결과 카드+예시 힌트, 업무대화 탭). 검증: 발화표(§2.1) 단위 13(파서 7+분류 6) + 통합 7(등록→조회→삭제·문서인식·도움말·멀티인텐트·지식 graceful). LLM 보조 라우팅은 후속.
- **F2 — 근거 LLM 합성 + 최신본 판별 ✅ 구현됨**: 지식 인텐트가 지식폴더(P2) 검색 결과를 (1) `version_ranker` 로 파일명 날짜·버전·'최종' 표식을 읽어 **최신본 판별**(같은 문서 다판본에서만 표시), (2) 로컬 LLM(`OllamaClient` 재사용)으로 **출처 표기가 붙은 근거 답변 합성**(best-effort, 실패 시 근거만). `OrchestratorService` 에 embedder/synthesizer 주입 → 테스트는 실 LLM 없이 결정적. `WorkChatPanel` 이 근거 답변 + 각 근거의 '최신본' 배지를 렌더. 대화→일정/문서 이어가기는 F1 에서 이미 실행(일정 등록·문서 인식→다운로드). 검증: 최신본 단위 5 + 오케스트레이터 지식 단위 2(주입). 스트리밍 합성은 후속.
- **F3 — 문서작성 엔진**: 양식 5종 + 개조식 구조 생성(LLM) + 미리보기/수정지시 + 승인 상태기계 + OWPML 양식 템플릿(현 `hwpx_generator` 확장). 종이 미리보기는 2차.
- **F4 — 지식폴더 심화 ①(추출·검색)**: HWP/HWPX·PDF·DOCX 추출(폐쇄망 wheelhouse 반입 검토: pypdf·python-docx·kordoc 대안) + FTS5 키워드 검색 + 상세검색 스트리밍.
- **F5 — 지식폴더 심화 ②(위키)**: 분류체계 마법사 + 지식위키 트리·문서카드·업무허브·버전 가족 + 증분(이동 승계·소프트삭제).
- **F6 — 일정 캘린더/알림**: 월/주/일 그리드 뷰 + 사전 알림(옵션·🔔) + 인앱 배너/토스트/우측 배지.
- **F7 — 전역 셸 UX**: 우측 컨텍스트 패널(승인·최근작업·가까운일정·색인로그) + 헤더(배율·엔진상태) + 첫 실행 온보딩 + 홈 브리핑 심화(이어서 하기·이용팁).
- **F8 — 환경설정 프로필 + 통합 QA**: LLM 프로필 전환(관리자 연결 위 사용자 선택) + 성능 예산 + 브라우저 E2E + 각 phase 보고서 취합 + major 릴리스.

---

## 5. 신규 기술 결정 (선결 조사 필요)

| 주제 | 결정/조사 |
|---|---|
| **문서 양식 개조식 구조** | LLM(기존 Ollama/OpenAI)로 초안 구조 생성 + 규칙 기반 위계 기호 정규화. 양식별 OWPML 템플릿에 주입 |
| **HWP/PDF/DOCX 추출** | 폐쇄망 wheelhouse 반입 대상 조사: `pypdf`(BSD)·`python-docx`. HWP/HWPX 는 `python-hwpx`(Apache2, gongmuwon 참조) 또는 자체 OWPML 파서. **오프라인 ZIP 정책상 wheel 반입 승인 필요** |
| **키워드 검색** | SQLite **FTS5**(트라이그램, 한국어) — 현 코사인(의미검색)과 병행 |
| **종이 미리보기** | @rhwp/core(WASM)는 gongmuwon 참조. AeroOne 은 우선 서버 렌더 근사(HTML) → WASM 은 2차 |
| **알림** | 폐쇄망이라 인앱 전용(배너·토스트·배지) + 브라우저 Notification(옵션). 이메일/푸시 없음 |
| **승인형 정책** | 문서 최종 저장 등 되돌리기 어려운 작업에 '대기 중인 승인' 상태 — AeroOne 감사 로그와 정합 |

---

## 6. 위험 · 제약

| 위험 | 대응 |
|---|---|
| 오프라인 ZIP 대용량(HWP/PDF wheel, WASM) | `installer-policy.json` 제외/포함 정책 재검토, 반입 승인 게이트 |
| 폐쇄망 순도 | 추출·색인·문서생성 전부 로컬. 외부 임베딩/네트워크 0 유지 |
| 라이선스 | gongmuwon 코드 이식 금지. 참조 라이브러리(python-hwpx Apache2, pypdf BSD, kordoc MIT) 개별 라이선스 준수 |
| 범위(사실상 앱 하나) | 단일 릴리스 금지. F1~F8 다중 phase, 각 phase 보고서(AGENTS §9.6) |
| 한컴 실기 검증 | HWPX 서식은 한컴 설치 PC 실기 확인(운영자 액션) 선행 후 서식 확정 |

---

## 7. 검증 전략

- 각 F단계: 백엔드 단위+통합 테스트, 프런트 tsc 0 + 컴포넌트 테스트, 실 Ollama 필요분 실증(F1 인텐트, F2 근거인용).
- 오케스트레이터: gongmuwon 발화표(§2.1)를 **회귀 케이스로 고정**(발화→인텐트→동작).
- 문서 양식: OWPML 구조 유효성 + 양식별 위계 기호 스냅샷. 한컴 실기 호환은 운영자 실기 게이트.
- 릴리스: 전체 게이트(backend pytest·frontend vitest·오프라인 ZIP) + phase 보고서.

---

## 8. 결론

현재는 **골격(breadth)** 만 섰다. 완전 파리티는 **F1 인텐트 오케스트레이터**(정체성)부터 채워야 하며, F2(근거인용·이어가기)·F3(문서 양식 엔진)·F4~F5(지식위키)가 실무 가치를 만든다. F6~F8(캘린더·셸 UX·프로필)이 마감이다. 신규 대용량 의존(HWP/PDF 추출·WASM)은 **오프라인 ZIP 반입 승인**을 선결하고, HWPX 서식은 한컴 실기 검증을 게이트로 둔다. 전체는 **major(2.0.0)** 트랙으로 다중 phase 진행한다.
